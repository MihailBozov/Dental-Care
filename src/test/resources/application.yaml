

spring:
  main:
    allow-bean-definition-overriding: on
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/website?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: 1212

  jpa:
    open-in-view: on
    database-platform: org.hibernate.dialect.MySQL8Dialect
    defer-datasource-initialization: on
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: never

  mvc:
    hiddenmethod:
      filter:
        enabled: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: mihailbozov001@gmail.com
    password: rxlj jsax eyos cmuz
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true

      
remember:
  me:
    key: ${REMEMBER_ME_KEY:topSecret}
    

image:
  upload:
    formNewTreatmentDir: "C:\\Users\\MihailBozov\\IdeaProjects\\website\\src\\main\\resources\\static\\images\\treatments"
